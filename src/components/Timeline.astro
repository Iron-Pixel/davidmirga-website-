---
const steps = [
  {
    year: "2008 - 2012",
    title: "Das Handwerk",
    role: "Maurer & Praxis",
    desc: "Gelernt, was harte Arbeit bedeutet. Probleme lösen, nicht diskutieren. Das Fundament für alles.",
    icon: "🧱"
  },
  {
    year: "2013 - 2018",
    title: "Der Verkauf",
    role: "Sales Profi (Vodafone)",
    desc: "Gelernt, Menschen zu verstehen und Lösungen zu verkaufen, die ihnen wirklich helfen.",
    icon: "💼"
  },
  {
    year: "2019 - 2023",
    title: "Die Agentur",
    role: "Social Recruiting (Addvengers)",
    desc: "Prozesse skaliert. HR digitalisiert. Gesehen, wo KMUs wirklich der Schuh drückt.",
    icon: "🚀"
  },
  {
    year: "Heute",
    title: "Die KI-Strategie",
    role: "Consultant & Trainer",
    desc: "Ich verbinde die Praxis aus dem Handwerk mit der Effizienz von KI. Keine Theorie, nur PS auf der Straße.",
    icon: "🤖"
  }
];
---

<div class="relative max-w-3xl mx-auto py-10">
  <!-- Vertikale Linie -->
  <div class="absolute left-4 md:left-1/2 top-0 bottom-0 w-0.5 bg-brand-navy/20"></div>

  <div class="space-y-12">
    {steps.map((step, index) => (
      <div class={`relative flex items-center justify-between md:justify-normal ${index % 2 === 0 ? 'md:flex-row-reverse' : ''}`}>
        
        <!-- Punkt auf der Linie -->
        <div class="absolute left-4 md:left-1/2 w-4 h-4 rounded-full bg-brand-red border-4 border-white shadow transform -translate-x-1.5 md:-translate-x-2"></div>

        <!-- Leere Hälfte für Desktop (damit es alterniert) -->
        <div class="hidden md:block w-5/12"></div>

        <!-- Content Box -->
        <div class="pl-12 md:pl-0 md:w-5/12">
          <div class={`p-6 bg-white rounded-xl shadow-sm border border-slate-100 hover:shadow-md transition-shadow relative ${index % 2 === 0 ? 'md:text-left' : 'md:text-right'}`}>
            <!-- Pfeil für Desktop -->
            <div class={`hidden md:block absolute top-6 w-4 h-4 bg-white border-b border-l border-slate-100 transform rotate-45 ${index % 2 === 0 ? '-left-2' : '-right-2 border-r border-t border-b-0 border-l-0'}`}></div>
            
            <span class="inline-block px-3 py-1 bg-brand-light text-brand-navy text-xs font-bold rounded-full mb-2">
              {step.year}
            </span>
            <div class="text-3xl mb-2">{step.icon}</div>
            <h3 class="text-xl font-heading font-bold text-brand-navy">{step.title}</h3>
            <div class="text-sm font-bold text-brand-red mb-2">{step.role}</div>
            <p class="text-slate-600 text-sm leading-relaxed">
              {step.desc}
            </p>
          </div>
        </div>

      </div>
    ))}
  </div>
</div>